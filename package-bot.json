{
  "name": "telegram-bot-only",
  "version": "1.0.0",
  "scripts": {
    "start": "cd bot && npm install && npm start",
    "build": "cd bot && npm install"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
